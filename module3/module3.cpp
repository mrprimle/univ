#include <iostream>
using namespace std;


int main()
{
    // PYTHON (no time, sorry)
    /*
    def noMin(arr):
        return all(elem > 0 for elem in arr)

    def module3(matrix):
        for row in matrix:
            if noMin(row):
                res = 1
                for el in row:
                    res *= el
                print(res)
                    
      
     */


    return 0;
}
